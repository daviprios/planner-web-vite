const ContentTemplate = (props: { children: JSX.Element }) => {
  return (
    <article>
      <section>
        
      </section>
      <section>

      </section>
    </article>
  )
}

export default ContentTemplate
